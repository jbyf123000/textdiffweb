services:
  textdiff-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: textdiff-web
    environment:
      PORT: 8080
    ports:
      - "8080:8080"
    restart: unless-stopped
